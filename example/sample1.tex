\documentclass{amsart}

\usepackage{sample_macros}

\begin{document}

\section{Main Document}

Here we'll use a macro: $\firstMacro$.

And then we can use a nested one: $\nestTop$.

\input{sample2.tex}

\end{document}